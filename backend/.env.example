CEREBRAS_API_KEY=your-cerebras-key
GROQ_API_KEY=your-groq-key
GEMINI_API_KEY=your-gemini-key
# MiniMax M2.7 — 229B MoE, SOTA real-world engineering (get key at platform.minimax.io)
MINIMAX_API_KEY=your-minimax-key
NEWS_API_KEY=your-newsapi-key
TAVILY_API_KEY=your-tavily-api-key
SERPAPI_KEY=your-serpapi-key
GOOGLE_FACTCHECK_API_KEY=your-google-factcheck-key
# ── ML Servers (100% Free Multi-Server Architecture) ─────────
# ML Server 1: Oracle Cloud - DeBERTa (most accurate, primary)
ML_SERVER_1_URL=http://your-ml-server-1-ip:8001
# ML Server 2: HuggingFace Spaces - Ensemble (backup)
ML_SERVER_2_URL=https://your-username-ml-server-2.hf.space
# Shared API key for ML servers (generate with: openssl rand -hex 32)
ML_API_KEY=your_secure_random_key_here

# ── Database (Neon PostgreSQL — free tier) ───────────────────
# Get from: neon.tech → your project → Connection → Connection string
#
# Use POOLED URL for the app (pgBouncer handles connection limits):
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
#
# If you need a separate migration URL (Alembic needs direct, not pooled):
# DATABASE_MIGRATION_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
#
# The app auto-creates tables and runs Alembic on every deploy.
# Nothing to run manually.
JWT_SECRET=your-random-secret
GOOGLE_CLIENT_ID=your-google-client-id
SMTP_USER=your-gmail@gmail.com
BREVO_API_KEY=your-brevo-api-key

# ── ML Models (Legacy - use ML Servers instead) ──────────────
# These are only used if ML servers are not configured
# Leave empty to skip local transformer loading (recommended for free tier)
DEBERTA_MODEL=
FORCE_TRANSFORMER_LOAD=false

# Enable fine-tuned ensemble (both RoBERTa + DeBERTa fine-tuned models)
# Run backend/training/train_finetune_ensemble.py first to generate ensemble_config.json
ENABLE_ENSEMBLE=false

# Force load transformer even on low-RAM environments (not recommended on free tier)
FORCE_TRANSFORMER_LOAD=false

# HuggingFace token (for downloading private/gated models)
HF_TOKEN=your-hf-token

# ── Phase 2.4: Social Graph Analysis (Optional) ──────────────
# Enable social media spread tracking
SOCIAL_GRAPH_ENABLED=false

# Twitter/X API v2 (Bearer Token from Developer Portal)
# Get from: https://developer.twitter.com/en/portal/dashboard
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# Reddit API (Create app at https://www.reddit.com/prefs/apps)
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=FactCheckerAI/1.0

# ── Wikidata entity verification (optional, disabled by default) ──
WIKIDATA_ENABLED=false

# ── Redis cache (optional, improves performance significantly) ────
# REDIS_URL=redis://localhost:6379/0
# REDIS_ENABLED=false
